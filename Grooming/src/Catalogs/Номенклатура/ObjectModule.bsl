
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	Если ТипНоменклатуры <> Перечисления.ТипыНоменклатуры.Услуга Тогда
		НомерУдаляемого = ПроверяемыеРеквизиты.Найти("ДлительностьУслуги");
		Если НомерУдаляемого <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(НомерУдаляемого);
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры
